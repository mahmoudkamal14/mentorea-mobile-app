import 'package:flutter/material.dart';

class MentoreaApp extends StatelessWidget {
  const MentoreaApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      debugShowCheckedModeBanner: false,
    );
  }
}
